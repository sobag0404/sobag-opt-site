# Next Goal Prompt

Use this prompt when starting the next goal-mode run for Sobag Opt.

```text
Включи goal mode для Sobag Opt. Отвечай по-русски и кратко.

Цель: сначала закрыть P0/P1 замечания по текущей VPS-версии Sobag Opt и довести ее до стабильного коммерческого состояния. Новый стек пока НЕ внедрять, но после закрытия этих пунктов напомнить пользователю про будущий переход без Node.js на этом же VPS или более мощном VPS.

Контекст:
- repo: https://github.com/sobag0404/sobag-opt-site
- branch: main
- production: https://sobag-shop.online
- VPS: 77.239.107.164
- текущая архитектура: HTML/CSS/JS + Node.js API на VPS
- текущий известный HEAD: c90bfeb Polish catalog header and footer pages; сначала подтянуть актуальный main
- сначала выполнить: git pull origin main
- перед изменениями: git status --short --branch
- не трогать production data/env/cache без отдельного подтверждения
- Vercel полностью исключаем из дальнейшего целевого контекста: не fallback, не deploy, не dev, не API runtime, не storage target

Правило работы:
- Двигаться малыми безопасными срезами.
- Не переписывать проект на новый стек в этом goal.
- Не выдумывать реальные юридические реквизиты, адреса, телефоны или карты.
- Не добавлять секреты, .env, токены, cookies, SSH-ключи, дампы БД.
- Коммитить только завершенные зеленые срезы.

P0/P1 исправления:

1. Custom print / briefForm
- Убрать фейковый успех формы “Изделия с вашим принтом”.
- Лучший вариант: сделать реальную серверную заявку, сохраняемую на VPS и видимую админу/менеджеру.
- Заявка должна иметь понятный тип, контакты, изделие, тираж, комментарий/макет-референс и статус.
- Если полноценная заявка слишком большая для первого среза, временно убрать форму и заменить честным CTA в корзину/контакты.
- Нельзя показывать успешную отправку без server save.

2. Корзина ниже 30 000 ₽
- Если минимум не набран, скрыть или полностью disabled весь checkout-блок.
- Показать сколько осталось до минимума.
- Не должно быть активной формы отправки при disabled checkout.
- Серверную проверку минимума оставить.

3. Privacy/legal
- Добавить `privacy.html` с production-safe текстом без фейковых реквизитов.
- Footer должен отдельно вести на “Политика конфиденциальности” и “Согласие на обработку персональных данных”.
- Existing consent PDF можно оставить для согласия.
- Обновить sitemap/footer/content mappings.

4. Контакты/карты
- Если юр. адрес или адрес производства не заполнен реальными данными, не строить карту по заглушке.
- Показывать “адрес уточняется”, карту скрывать или disabled.
- В админке оставить редактирование юр. адреса и адреса производства.
- Не придумывать реальные адреса.

5. Mobile UX
- Сократить мобильный хедер.
- Оставить доступными: логотип/каталог, поиск, вход/аккаунт, избранное, корзина.
- Убрать лишний текст корзины/верхней строки на малых экранах.
- Проверить отсутствие horizontal overflow.

6. Карточка товара
- Default quantity = 1.
- Если quantity = 0, add-button disabled.
- Матрицу вариантов не ломать.

7. Промокод
- Если реальных промокодов нет, убрать активный ввод или заменить на “по согласованию с менеджером”.
- Не создавать видимость работающей скидки.

8. Hero/H1
- Исправить плохой перенос H1, особенно одинокую строку “С”.
- Без большого редизайна: только аккуратная responsive typography и ограничения ширины.

9. Коллекции
- Для длинного списка коллекций добавить “Показать еще” или компактную группировку.
- Не ломать поиск, фильтры и SEO.

После закрытия P0/P1:
- Напомнить пользователю о будущем переходе без Node.js.
- Целевой будущий стек:
  Backend: Rust Axum
  HTML/SSR: Rust templates + HTMX
  DB: PostgreSQL
  Cache/session: Redis
  Search: Meilisearch
  Files/photos: MinIO/S3-compatible
  Deploy: Docker Compose или systemd на VPS
  Nginx остается reverse proxy
- Миграцию делать позже параллельно, не ломая текущий сайт.

Проверки:
- После крупных блоков: npm.cmd run check
- Перед push: npm.cmd run check и npm.cmd run ui:smoke
- После push дождаться Actions: autofix-check, vps-deploy, production-smoke
- Проверить:
  npm.cmd run smoke:prod -- --base-url https://sobag-shop.online
- Обновить docs/ai-handoff/ACTIVE_CONTEXT.md, docs/ai-handoff/CURRENT_STATUS.md, docs/roadmap-checklist.md, project-ai-handoff-latest.zip
- Коммитить только зеленый завершенный срез.
```
