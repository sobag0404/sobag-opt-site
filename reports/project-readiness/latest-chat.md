Next implementation packet:
1. Verified coordinator baseline: deploy workflow YAML fixes `0ace8a6`, `c2c1b56`, `637f20d`; `vps-deploy` `27675967030` PASS and `production-smoke` `27676234502` PASS. Live anonymous checks: `/api/health` 200, `/api/admin/catalog` 401, `/api/admin/import-batches` 401, `/api/catalog-query?pageSize=1` 200, `/api/price-list?format=json` 200.
2. Current media slice: Rust `/rust/admin/product-images` parity is implemented for admin/content GET, JSON base64 upload, mark-unused, and delete through S3-compatible object storage. Guards: supported image MIME, JSON size cap, storage keys confined to `products/*`, path traversal rejection, no secret/cookie logging.
3. Deploy gate for media is prepared: exact `/api/admin/product-images -> /rust/admin/product-images` only, timestamped Nginx backup `/etc/nginx/sites-available/sobag-opt.pre-rust-admin-media-*`, anonymous 401, then live temporary upload/list/delete smoke with cleanup. No broad `/api` or `/api/admin` proxy.
4. Local checks to rerun/confirm before finalizing: `cargo fmt --check`, `cargo check --locked`, `cargo test --locked`, `npm.cmd run check`, `node tools/rust-admin-media-cutover-smoke.mjs --self-test`, `node tools/rust-account-route-rehearsal.mjs --group admin-media --self-test`, `git diff --check`.
5. If GitHub/VPS media deploy PASS, update runtime map as Rust cutover complete except any explicitly unverified import-batches write semantics. If media live smoke fails, rollback exact route from backup and keep Node fallback. UI remains out of scope.
Status: READY_WITH_WARNINGS. Score: 87/100. Report: reports/project-readiness/latest.md.
