Next implementation packet:
1. Rust cutover is complete for the targeted VPS exact routes after direct MinIO repair and manual deploy: `vps-deploy` `27816085213` PASS and `production-smoke` `27816499824` PASS. Production Rust ownership covers auth, orders/briefs, admin orders/users/content/PIM/prices/catalog/import-batches/product-images, public catalog/search/product/pages, content pages, and catalog query/detail APIs.
2. MinIO media fix: VPS MinIO data ownership was restored to the MinIO service user, media write/stat/delete was verified, the app env was updated on the VPS, and Rust was restarted. `tools/vps-minio-media-policy.sh` now has a safe allowlisted data-ownership repair guard so future deploy media gates can recover that class without printing secrets.
3. Live gates to keep watching: health, `/` canonical, `/index.html -> /`, catalog first-load facets (`Подушки 517`, `Наволочки 517`, `Мешки для обуви 170`, `Чехлы на чемодан 37`, `Ремувки 19`, `Флаги 65`), non-zero prices, product images, cache headers, admin media anonymous 401, and authenticated media upload/list/delete cleanup.
4. Remaining Node runtime dependency: Node stays only as static/root/cart compatibility fallback and explicit legacy fallback; do not remove it until a separate full no-Node static/cart plan is written and gated. Future destructive import business changes still need authenticated dry-run/apply/rollback evidence.
5. Post-cutover backlog is now scoped: grouped price-list export, admin CSV/Excel-compatible price import, promo rows, and buyer-only review eligibility are documented in `docs/backend-pricing-reviews-contract.md`. Latest pricing hardening: Node/Rust accept spaced Excel-style numeric prices, reject invalid promo windows, export promo rows only when date-active, and test rollback on DB apply failure. Promo order-pricing precedence and backend XLSX styling still need explicit business rules before changing order totals/output styling. Keep `npm.cmd run check`, `cargo fmt/check/test` via CI/VPS, GitHub `autofix-check`, `rust-check`, `vps-deploy`, and `production-smoke` green after every production-code change.
Status: READY_WITH_WARNINGS. Score: 90/100. Report: reports/project-readiness/latest.md.
