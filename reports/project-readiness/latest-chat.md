Next implementation packet:
1. Current state: production exact `/api/auth/*` writes, `/api/auth/me`, `/api/orders`, `/api/briefs`, `/api/admin/orders`, `/api/admin/users`, `/api/admin/content`, read-only `/api/admin/pim`, and `/api/admin/prices` are cut over to Rust. Latest admin-prices runtime-change release: `20260616T205347Z-32076fd`; later docs-only deploys may advance the VPS marker. Admin prices rollback backup: `/etc/nginx/sites-available/sobag-opt.pre-rust-admin-prices-20260616T205931Z`.
2. Evidence: GitHub `autofix-check` `27647385195`, `rust-check` `27647385766`, `vps-deploy` `27647440957`, and `production-smoke` `27647708702` passed for `32076fd`. Live admin prices smoke verified anonymous 401, temporary content-role list/preview/apply, zero-price rejection, non-zero catalog-detail prices, and cleanup.
3. Remaining Rust fallback: admin catalog/import/media mutation routes only. Do not touch UI; do not make broad Nginx regex/proxy changes. Continue route-by-route with parity map, tests, exact-route Nginx backups, live gates, and rollback.
4. Required checks for next cutover: `cargo fmt --check`, `cargo check --locked`, `cargo test --locked`, `npm.cmd run check`, targeted smokes for changed routes, GitHub `autofix-check`, `rust-check`, `vps-deploy`, `production-smoke`, plus live admin-safe validation.
5. P2/P3 warnings only: `rust-server/src/main.rs` large-file ownership, optional ESLint/SHA-pinning, and real field CWV post-launch monitoring. Keep Vercel/Next absent and never print secrets/cookies/tokens.
Status: READY_WITH_WARNINGS. Score: 87/100. Report: reports/project-readiness/latest.md.
