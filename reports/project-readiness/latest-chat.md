Next implementation packet:
1. Current state: production exact `/api/auth/*` writes, `/api/auth/me`, `/api/orders`, `/api/briefs`, `/api/admin/orders`, `/api/admin/users`, `/api/admin/content`, read-only `/api/admin/pim`, and `/api/admin/prices` are verified Rust cutovers. Latest verified admin-prices runtime release: `20260616T205347Z-32076fd`; backup: `/etc/nginx/sites-available/sobag-opt.pre-rust-admin-prices-20260616T205931Z`.
2. New admin catalog work: Rust parity is pushed in `240e423`; deploy-gate exact `/api/admin/catalog -> /rust/admin/catalog` switch is pushed in `7e8d225` with a timestamped Nginx backup and anonymous-401 guard. This local shell cannot reach GitHub Actions/live domain, so verify Actions/VPS before marking catalog PASS.
3. New import work: Rust `/rust/admin/import-batches` parity is pushed in `1c2a72e` for admin/content list, preview, reject, apply, and latest-batch rollback. Local checks passed: `cargo fmt --check`, `cargo test --locked admin_import_batch`, import-batches self-test, route rehearsal self-test, `npm.cmd run check`, `git diff --check`. Public `/api/admin/import-batches` remains Node until live dry-run/apply/rollback gates exist.
4. Next steps: first verify `7e8d225`/`1c2a72e` on GitHub/VPS (`autofix-check`, `rust-check`, `vps-deploy`, `production-smoke`) and live `/api/admin/catalog` route status. If catalog fails, rollback exact route from backup. If PASS, record evidence and then add import-batches live smoke before any route switch.
5. Remaining Rust fallback: import-batches public route until live gates, media mutation routes until upload/list/delete temp-file gates with MIME/size/path traversal cleanup. Do not touch UI, do not make broad Nginx regex/proxy changes, and never print secrets/cookies/tokens.
Status: READY_WITH_WARNINGS. Score: 87/100. Report: reports/project-readiness/latest.md.
