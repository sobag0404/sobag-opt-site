Next implementation packet:
0z. Security pass 2026-06-24: added `tools/security-posture-audit.mjs` plus `audit:security-posture`/AutoFix coverage for CI/CD secret-leak patterns, explicit workflow permissions, npm lockfile source/integrity checks, tracked secret-like file detection, and redacted web-log attack-class self-test. Local advisory `npm audit` is network-blocked; run a network-enabled advisory audit after deploy if needed.
0y. Final frontend accessibility/SEO/state pass: cart checkout and promo fields now expose explicit accessible names, public customer pages missing static canonicals were filled (`cart`, `custom`, `favorites`, `marketplaces`, `terms`), and UI smoke now guards customer page canonicals plus unnamed cart form controls. Browser QA covered customer flows at 390/768/1366 with no overflow or stale `48`.
0x. Final customer-flow UI pass: cart line remove controls keep a visible fallback/title when icons are unavailable, checkout modal uses a tighter cart-specific panel width/backdrop, and styles/cart cache-busts advance to `20260624-customer-flow`. Local Playwright QA covered home/catalog/listing/search/product modal/favorites/cart/checkout/account at 390/768/1366; category -> catalog stayed on full counts with no stale `48`.
0w. Backend/admin hardening after current slice: Rust admin import-batch `updateExisting` preview preserves product id, visibility/status, image/gallery/media metadata, and SKU override price metadata when omitted from import rows; preview also guards variant SKU collisions before apply. Local coverage passed: targeted Rust unit, import-batches cutover self-test, backend evidence self-test, API security smoke, price-groups smoke, `cargo fmt --check`, `cargo check --locked`, `npm.cmd run check`, and `git diff --check`.
0v. Final production speed/images slice: product responsive media now uses scoped sizes for cards (260px cap), detail images (520px cap), and thumbnails (80px), with cache-busts advanced to `20260624-product-image-sizes`. `tools/frontend-perceived-speed-smoke.mjs` guards responsive source sizes plus catalog speed; `tools/production-smoke.mjs` now checks the current app/product-utils cache-busts. Live production/browser and GitHub CLI checks remain blocked from this shell by network/auth (`ERR_NETWORK_ACCESS_DENIED` / unauthenticated `gh`).
0u. Final frontend page speed closure: catalog home no longer renders a hidden 48-card product grid, full product/catalog loading is deferred on catalog home/static pages, validated full product loads write a home-summary cache, and `tools/frontend-perceived-speed-smoke.mjs` now guards cold/repeat/SPА catalog speed. Local smoke: catalog home ready 103-198ms, category first cards 444-474ms, repeat catalog 36ms, SPA category->catalog 54ms, no stale `48 товаров`, no overflow.
0t. P0 frontend perceived-load slice: catalog/product first-screen product images now load eager/high priority for the first four cards, later cards stay lazy, and mobile/tablet header layout separates Catalog and search rows so SPA catalog navigation is clickable. Local Playwright perf sample at 390/768/1366: catalog/category visible with 6 category tiles + 48 cards in ~0.5s, no stale `48 товаров`, no horizontal overflow.
0s. Static content-page polish: contacts/marketplaces/delivery/payment/returns/business/how-to-order/wholesale/seller-support static links now expose clear labels/titles for back/contact/CTA actions; contacts email/phone/map links use existing button styling for mobile tap targets. Targeted browser audit at 390/768/1366 passed with no overflow/clipping/link-attribute issues. Full check pending.
0r. P0 frontend cache model after `6a5cab1`: public catalog API cache uses request-shape scoped keys, old unscoped public/cache-summary entries are purged without clearing private cart/favorites/profile/order state, catalog-home local counts are allowed only from a trusted full product cache, and validated home-summary counts remain visible during failed background revalidation. Catalog HTML cache-bust points at app `20260624-cache-revalidate` plus data/content `20260624-public-cache-v3`; local check + UI smoke + diff-check pass.
0q. P0 catalog SPA navigation after `c57b0e9`: category listing -> header `Каталог` now rechecks/loads the home summary when boot started inside listing, so catalog home cannot reuse page-sized server listing counts. Added smoke that mocks `Подушки` listing with `48` facet rows, navigates SPA back to catalog home, asserts no `48 товаров`, and waits for full `517` summary counts. App/styles cache-bust is `20260624-catalog-spa-home`; local check + UI smoke + diff-check pass.
0p. UI catalog filter controls after `424c1a1`: removed the prominent top `Снять все фильтры` button from active chips, added sidebar `Применить фильтр` / `Сбросить фильтр`, and made sidebar reset keep the user in listing via in-place `view=list` route updates without document reload. App/styles cache-bust is `20260624-filter-actions`; local check + UI smoke + diff-check pass.
0o. P0 catalog performance after `c103f28`: live `/catalog.html` is still unreachable from this shell, but local first-load root cause was narrowed to category home blocking on summary API. The UI now renders real category tiles immediately with neutral nonnumeric count status, uses cached full summary counts when safe, keeps rejecting page-sized `48 товаров`, and cache-busts app/styles to `20260623-category-fast`. Local timing sample: DOM 121ms, first category tiles 446ms, counts ready 458ms, no `48`; local check + UI smoke + diff-check pass.
0n. P0 catalog live follow-up after `8888706`: live `/catalog.html` remains unreachable from this shell (`Невозможно соединиться с удаленным сервером`), so the local production-like guard was kept strict: stale page-sized `48 товаров` is blocked before API, category tiles expose API-derived count labels/titles, app cache-bust is `20260623-category-title`, and production smoke reports safe count diagnostics. Local check + UI smoke + diff-check pass.
0m. UI continuation after `270cc8c`: product and admin content modals now expose explicit close-button aria labels/titles, with app/admin cache-busts updated and smoke coverage added. Local check + UI smoke + diff-check pass.
0l. UI continuation after `f460f3b`: contacts map links now expose explicit hover/focus titles with the resolved address while preserving safe external map links; app cache-bust updated. Local check + UI smoke + diff-check pass.
0k. UI continuation after `6ebc90b`: catalog category skeleton/ready container now exposes aria-busy/live/label status so cold-load waits are intentional without showing stale counts; app cache-bust updated. Local check + UI smoke + diff-check pass.
0j. UI continuation after `c7ac0e2`: product card media buttons and gallery thumbnails now include matching titles for hover/focus discoverability; app cache-bust updated. Local check + UI smoke + diff-check pass.
0i. UI continuation after `ee1fdc7`: checkout button now exposes locked/ready aria-disabled state and a title tied to the min-order hint; cart.js cache-bust updated. Local check + UI smoke + diff-check pass.
0h. UI continuation after `65c1d73`: review login CTA now has explicit label/title, eligible review form and textarea reference moderation help text, and smoke covers anonymous/eligible accessibility. Local check + UI smoke + diff-check pass.
0g. UI continuation after `c70ae7b`: price-list download/status UX now marks CSV format on catalog/detail download links and announces empty/error price preview states; local check + UI smoke + diff-check pass.
0f. UI continuation after `801f56a`: admin price preview empty/validation states now announce status politely; local check + UI smoke + diff-check pass. Live check remains blocked from this shell.
0e. UI continuation after `3e5acb8`: marketplace cards/generated link groups now keep explicit aria labels/titles for external links; local check + UI smoke + diff-check pass.
0d. UI continuation after `f8f0ac0`: product media now has explicit broken-image fallback handling and app/product-helper cache-busts; local check + UI smoke + diff-check pass.
0c. UI continuation after `f08baa3`: cart min-order and quote actions now have explicit status/aria/title coverage; local check + UI smoke + diff-check pass.
0b. UI continuation after `0d18fd8`: review eligibility notices now use polite status regions for anonymous/ineligible/already-reviewed states; local check + UI smoke pass.
0a. UI continuation after `9693839`: price-list download links now use stable `sobag-price-list.csv`, explicit CSV accessibility text, and a short busy/toast state; local check + UI smoke pass, live/GitHub still blocked from this shell.
0. UI continuation after `43e8155`: admin price import apply now asks for explicit confirmation before applying previewed local/server changes; local check + UI smoke pass, while live/GitHub checks remain blocked from this shell.
1. Rust cutover remains complete for the targeted VPS exact routes. Latest pre-slice verified baseline: main `eca1c83`, `autofix-check` `27873133522` PASS, `rust-check` `27873133515` PASS, `vps-deploy` `27873157596` PASS, and `production-smoke` `27873366515` PASS.
1a. Deploy/cache hardening: catalog HTML now references the current app.js cache-bust, production smoke checks `/catalog.html` for that cache-bust value, and the catalog-home summary guard rejects page-sized partial counts such as stale `48 товаров` with safe total/pageSize/maxCategoryCount diagnostics.
1b. Backend hardening: price import smoke now covers admin-only template/CSV preview/apply/history and duplicate SKU rejection; production smoke covers JSON+CSV price-list and rejects page-limited category counts.
1c. Admin price import rate-limit smoke now covers `/api/admin/prices` burst writes: validation remains `400 unknown_action` before the limiter and becomes `429 rate_limited` after abuse.
1d. Production smoke now checks anonymous denial across representative Rust-owned protected routes: `/api/auth/me`, admin catalog/content/import/orders/prices/product-images/users.
1e. VPS release audit now requires backend evidence smoke and `backup:evidence` scripts so no-secret evidence checks stay in deployable releases.
1f. Price import smoke now proves SKU-only override preview targets one SKU and promo import apply records promo-change history.
1g. Production smoke now rejects any zero/invalid price-list row in JSON or CSV responses, not only the first row.
1h. CSV/XLSX safety: admin price CSV route smoke rejects formula-like SKU input; docs clarify backend is CSV/parsed rows and XLSX parsing stays client-side.
1i. Review eligibility smoke now rejects completed-order buyers reviewing a different product/SKU; eligibility stays tied to purchased items.
1j. Account/order smoke now explicitly rejects anonymous account/cart writes and anonymous buyer order PATCH before ownership/conflict tests.
1k. Production smoke now rejects aggressively cached HTML and requires public cache-control on price-list JSON/CSV.
1l. Backend evidence smoke now supports strict no-secret packets for post-Rust PostgreSQL/MinIO evidence summaries.
2. Current backend hardening slices are ready for commit/deploy: audit summary compatibility now handles legacy string actors and structured actor objects; content saves append safe `content_update`; account/order ownership now uses `userEmail` only, so customer contact email cannot claim account order history, order comments, or review eligibility.
3. Rate-limit hardening covers Rust account/order/brief/admin-user/admin-content writes and Node compatibility admin catalog/import/media/price/content/order/user mutations through the shared store-backed limiter with documented memory fallback. Account sync hardening returns `favoritesUpdatedAt` / `savedCartsUpdatedAt` and rejects stale favorites/saved-cart writes with sanitized `409` conflict payloads.
4. Pricing/promo preservation now has an explicit guard: order totals continue to use trusted base catalog prices even when promo fields exist. Promo order-pricing precedence remains a business-rule decision.
5. Backup/evidence and production smoke hardening added media-image metadata evidence plus live-safe smoke checks for canonical `/index.html -> /`, catalog-query non-zero prices, public price-list rows, and anonymous admin media denial. Keep watching live gates after deploy: health, catalog facets, images, cache headers, and admin media upload cleanup. Remaining business decision: backend XLSX styling.
6. Rust parity cleanup updated the account/orders/admin cutover runbook and audit to the completed exact-route map; no targeted API route remains in the old "do not switch yet" state.
7. Buyer review protection now has a dedicated AutoFix contract audit for Node/Rust eligibility markers and smoke coverage: anonymous/no-order/other-user/pending/canceled/duplicate are denied, completed-order buyer review is allowed, and burst writes return `429`.
Status: READY_WITH_WARNINGS. Score: 90/100. Report: reports/project-readiness/latest.md.
