Next implementation packet:
1. Current state: production exact `/api/auth/*` writes, `/api/auth/me`, `/api/orders`, `/api/briefs`, `/api/admin/orders`, `/api/admin/users`, `/api/admin/content`, and read-only `/api/admin/pim` are cut over to Rust. Latest verified release: `20260616T192544Z-8907ac2`; admin PIM backup: `/etc/nginx/sites-available/sobag-opt.pre-rust-admin-pim-20260616T193119Z`.
2. Evidence: GitHub `autofix-check` `27642409965`, `rust-check` `27642410002`, `vps-deploy` `27642450650`, and `production-smoke` `27642729431` passed for `8907ac2`. Live admin PIM smoke verified anonymous 401, temporary content-role summary/variants/CSV through Rust, non-zero prices for 12,943 variants, invalid-view 400, and cleanup.
3. Remaining Rust fallback: admin catalog/import/media/price mutation routes only. Do not touch UI; do not make broad Nginx regex/proxy changes. Continue by inventorying each mutation route, adding parity tests/smokes, backing up Nginx before each exact route switch, and keeping rollback ready.
4. Required checks for next cutover: `cargo fmt --check`, `cargo check --locked`, `cargo test --locked`, `npm.cmd run check`, targeted smokes for changed routes, GitHub `autofix-check`, `rust-check`, `vps-deploy`, `production-smoke`, plus live admin-safe validation.
5. P2 warnings only: real field CWV remains post-launch monitoring, ARCH-001 large-file ownership remains, and lint/guardrail cleanup is separate. Keep Vercel/Next absent and never print secrets/cookies/tokens.
Статус проекта: READY_WITH_WARNINGS. Оценка: 87/100. Отчёт: reports/project-readiness/latest.md.
